# Funkjtioniert erst wenn der course service fertig ist
name: "courses-api-abfrage"
description: "Holt Kursdaten aus der Kurse DB"

params:
  - name: COURSE_ID
    default: "11111111-1111-1111-1111-111111111111"

steps:
  - name: "fetch-course"
    command: "curl -sS -f -X GET http://traefik:8000/courses/${COURSE_ID}"